/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS `Complex_componentStoichiometry`;

DROP TABLE IF EXISTS `Complex_component`;

DROP TABLE IF EXISTS `Complex`;

CREATE TABLE `Complex` (
  `unipaxId` BIGINT UNSIGNED NOT NULL PRIMARY KEY)
 ENGINE=InnoDB;

CREATE TABLE `Complex_component` (
  `object_id` BIGINT UNSIGNED NOT NULL,
  `index` BIGINT UNSIGNED NOT NULL,
  `value` BIGINT UNSIGNED NULL,
  CONSTRAINT `Complex_component_object_id_fk`
    FOREIGN KEY (`object_id`)
    REFERENCES `Complex` (`unipaxId`)
    ON DELETE CASCADE)
 ENGINE=InnoDB;

CREATE INDEX `object_id_i`
  ON `Complex_component` (`object_id`);

CREATE INDEX `index_i`
  ON `Complex_component` (`index`);

CREATE TABLE `Complex_componentStoichiometry` (
  `object_id` BIGINT UNSIGNED NOT NULL,
  `index` BIGINT UNSIGNED NOT NULL,
  `value` BIGINT UNSIGNED NULL,
  CONSTRAINT `Complex_componentStoichiometry_object_id_fk`
    FOREIGN KEY (`object_id`)
    REFERENCES `Complex` (`unipaxId`)
    ON DELETE CASCADE)
 ENGINE=InnoDB;

CREATE INDEX `object_id_i`
  ON `Complex_componentStoichiometry` (`object_id`);

CREATE INDEX `index_i`
  ON `Complex_componentStoichiometry` (`index`);

ALTER TABLE `Complex`
  ADD CONSTRAINT `Complex_unipaxId_fk`
    FOREIGN KEY (`unipaxId`)
    REFERENCES `PhysicalEntity` (`unipaxId`)
    ON DELETE CASCADE;

/*
ALTER TABLE `Complex_component`
  ADD CONSTRAINT `Complex_component_value_fk`
    FOREIGN KEY (`value`)
    REFERENCES `PhysicalEntity` (`unipaxId`)
*/

/*
ALTER TABLE `Complex_componentStoichiometry`
  ADD CONSTRAINT `Complex_componentStoichiometry_value_fk`
    FOREIGN KEY (`value`)
    REFERENCES `Stoichiometry` (`unipaxId`)
*/

