/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS `DnaRegion_entityReference`;

DROP TABLE IF EXISTS `DnaRegion`;

CREATE TABLE `DnaRegion` (
  `unipaxId` BIGINT UNSIGNED NOT NULL PRIMARY KEY)
 ENGINE=InnoDB;

CREATE TABLE `DnaRegion_entityReference` (
  `object_id` BIGINT UNSIGNED NOT NULL,
  `index` BIGINT UNSIGNED NOT NULL,
  `value` BIGINT UNSIGNED NULL,
  CONSTRAINT `DnaRegion_entityReference_object_id_fk`
    FOREIGN KEY (`object_id`)
    REFERENCES `DnaRegion` (`unipaxId`)
    ON DELETE CASCADE)
 ENGINE=InnoDB;

CREATE INDEX `object_id_i`
  ON `DnaRegion_entityReference` (`object_id`);

CREATE INDEX `index_i`
  ON `DnaRegion_entityReference` (`index`);

ALTER TABLE `DnaRegion`
  ADD CONSTRAINT `DnaRegion_unipaxId_fk`
    FOREIGN KEY (`unipaxId`)
    REFERENCES `PhysicalEntity` (`unipaxId`)
    ON DELETE CASCADE;

/*
ALTER TABLE `DnaRegion_entityReference`
  ADD CONSTRAINT `DnaRegion_entityReference_value_fk`
    FOREIGN KEY (`value`)
    REFERENCES `EntityReference` (`unipaxId`)
*/

