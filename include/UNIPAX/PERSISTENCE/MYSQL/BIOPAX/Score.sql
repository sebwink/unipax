/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS `Score_comment`;

DROP TABLE IF EXISTS `Score`;

CREATE TABLE `Score` (
  `unipaxId` BIGINT UNSIGNED NOT NULL PRIMARY KEY,
  `unipaxSource` BIGINT UNSIGNED NULL,
  `typeid` VARCHAR(255) NOT NULL,
  `scoreSource` BIGINT UNSIGNED NULL,
  `value` TEXT NOT NULL)
 ENGINE=InnoDB;

CREATE TABLE `Score_comment` (
  `object_id` BIGINT UNSIGNED NOT NULL,
  `index` BIGINT UNSIGNED NOT NULL,
  `value` TEXT NOT NULL,
  CONSTRAINT `Score_comment_object_id_fk`
    FOREIGN KEY (`object_id`)
    REFERENCES `Score` (`unipaxId`)
    ON DELETE CASCADE)
 ENGINE=InnoDB;

CREATE INDEX `object_id_i`
  ON `Score_comment` (`object_id`);

CREATE INDEX `index_i`
  ON `Score_comment` (`index`);

/*
ALTER TABLE `Score`
  ADD CONSTRAINT `Score_unipaxSource_fk`
    FOREIGN KEY (`unipaxSource`)
    REFERENCES `ImportSource` (`unipaxId`),
  ADD CONSTRAINT `Score_scoreSource_fk`
    FOREIGN KEY (`scoreSource`)
    REFERENCES `Provenance` (`unipaxId`)
*/

