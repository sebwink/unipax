/* This file was generated by ODB, object-relational mapping (ORM)
 * compiler for C++.
 */

DROP TABLE IF EXISTS `RnaRegionReference`;

CREATE TABLE `RnaRegionReference` (
  `unipaxId` BIGINT UNSIGNED NOT NULL PRIMARY KEY,
  `sequence` TEXT NOT NULL,
  `subRegion` BIGINT UNSIGNED NULL,
  `absoluteRegion` BIGINT UNSIGNED NULL,
  `organism` BIGINT UNSIGNED NULL,
  `regionType` BIGINT UNSIGNED NULL)
 ENGINE=InnoDB;

ALTER TABLE `RnaRegionReference`
  ADD CONSTRAINT `RnaRegionReference_unipaxId_fk`
    FOREIGN KEY (`unipaxId`)
    REFERENCES `EntityReference` (`unipaxId`)
    ON DELETE CASCADE
  /*
  ADD CONSTRAINT `RnaRegionReference_subRegion_fk`
    FOREIGN KEY (`subRegion`)
    REFERENCES `EntityReference` (`unipaxId`)
  */
  /*
  ADD CONSTRAINT `RnaRegionReference_absoluteRegion_fk`
    FOREIGN KEY (`absoluteRegion`)
    REFERENCES `SequenceLocation` (`unipaxId`)
  */
  /*
  ADD CONSTRAINT `RnaRegionReference_organism_fk`
    FOREIGN KEY (`organism`)
    REFERENCES `BioSource` (`unipaxId`)
  */
  /*
  ADD CONSTRAINT `RnaRegionReference_regionType_fk`
    FOREIGN KEY (`regionType`)
    REFERENCES `SequenceRegionVocabulary` (`unipaxId`)
  */;

