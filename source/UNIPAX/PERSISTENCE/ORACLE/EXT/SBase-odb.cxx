// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

// Begin prologue.
//
//
// End prologue.

#include <UNIPAX/PERSISTENCE/ORACLE/EXT/SBase-odb.hxx>

#include <cassert>
#include <cstring> // std::memcpy

#include <odb/cache-traits.hxx>
#include <odb/details/shared-ptr.hxx>

#include <odb/oracle/binding.hxx>
#include <odb/oracle/traits.hxx>
#include <odb/oracle/database.hxx>
#include <odb/oracle/transaction.hxx>
#include <odb/oracle/connection.hxx>
#include <odb/oracle/statement.hxx>
#include <odb/oracle/statement-cache.hxx>
#include <odb/oracle/object-statements.hxx>
#include <odb/oracle/container-statements.hxx>
#include <odb/oracle/exceptions.hxx>
#include <odb/oracle/result.hxx>

namespace odb
{
  // SBase
  //

  const char query_columns_base< ::UniPAX::SBase >::unipaxSource_alias_[] = "\"unipaxSource\"";

  void access::object_traits< ::UniPAX::SBase >::
  bind (oracle::bind* b, image_type& i, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    std::size_t n (0);

    // UPBase base
    //
    object_traits< ::UniPAX::UPBase >::bind (b + n, i, sk);
    n += 1UL - (
      sk == statement_select ? 0 : (
        sk == statement_insert ? 0 : 1UL));

    // unipaxSource
    //
    b[n].type = oracle::bind::number;
    b[n].buffer = i.unipaxSource_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.unipaxSource_value));
    b[n].size = &i.unipaxSource_size;
    b[n].indicator = &i.unipaxSource_indicator;
    n++;

    // metaId
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.metaId_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.metaId_value));
    b[n].size = &i.metaId_size;
    b[n].indicator = &i.metaId_indicator;
    n++;

    // sboTerm
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.sboTerm_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.sboTerm_value));
    b[n].size = &i.sboTerm_size;
    b[n].indicator = &i.sboTerm_indicator;
    n++;

    // notes
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.notes_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.notes_value));
    b[n].size = &i.notes_size;
    b[n].indicator = &i.notes_indicator;
    n++;

    // annotation
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.annotation_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.annotation_value));
    b[n].size = &i.annotation_size;
    b[n].indicator = &i.annotation_indicator;
    n++;
  }

  bool access::object_traits< ::UniPAX::SBase >::
  init (image_type& i, const object_type& o, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    if (i.change_callback_.callback != 0)
      (i.change_callback_.callback) (i.change_callback_.context);

    bool grew (false);

    // UPBase base
    //
    if (object_traits< ::UniPAX::UPBase >::init (i, o, sk))
      grew = true;

    // unipaxSource
    //
    {
      typedef object_traits< ::UniPAX::ImportSource > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::ImportSource > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (o.unipaxSource));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (o.unipaxSource)));

        std::size_t size (0);
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_image (
          i.unipaxSource_value,
          size,
          is_null,
          id);
        i.unipaxSource_size = static_cast<ub2> (size);
      }

      i.unipaxSource_indicator = is_null ? -1 : 0;
    }

    // metaId
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.metaId_value,
        sizeof (i.metaId_value),
        size,
        is_null,
        o.metaId);
      i.metaId_size = static_cast<ub2> (size);
      i.metaId_indicator = is_null ? -1 : 0;
    }

    // sboTerm
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.sboTerm_value,
        sizeof (i.sboTerm_value),
        size,
        is_null,
        o.sboTerm);
      i.sboTerm_size = static_cast<ub2> (size);
      i.sboTerm_indicator = is_null ? -1 : 0;
    }

    // notes
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.notes_value,
        sizeof (i.notes_value),
        size,
        is_null,
        o.notes);
      i.notes_size = static_cast<ub2> (size);
      i.notes_indicator = is_null ? -1 : 0;
    }

    // annotation
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.annotation_value,
        sizeof (i.annotation_value),
        size,
        is_null,
        o.annotation);
      i.annotation_size = static_cast<ub2> (size);
      i.annotation_indicator = is_null ? -1 : 0;
    }

    return grew;
  }

  void access::object_traits< ::UniPAX::SBase >::
  init (object_type& o, const image_type& i, database& db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // UPBase base
    //
    object_traits< ::UniPAX::UPBase >::init (o, i, db);

    // unipaxSource
    //
    {
      typedef object_traits< ::UniPAX::ImportSource > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::ImportSource > > ptr_traits;

      if (i.unipaxSource_indicator == -1)
        o.unipaxSource = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_value (
          id,
          i.unipaxSource_value,
          i.unipaxSource_size,
          i.unipaxSource_indicator == -1);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        o.unipaxSource = ptr_traits::pointer_type (
          db.load< obj_traits::object_type > (id));
      }
    }

    // metaId
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.metaId,
      i.metaId_value,
      i.metaId_size,
      i.metaId_indicator == -1);

    // sboTerm
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.sboTerm,
      i.sboTerm_value,
      i.sboTerm_size,
      i.sboTerm_indicator == -1);

    // notes
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.notes,
      i.notes_value,
      i.notes_size,
      i.notes_indicator == -1);

    // annotation
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.annotation,
      i.annotation_value,
      i.annotation_size,
      i.annotation_indicator == -1);
  }

  struct access::object_traits< ::UniPAX::SBase >::container_statement_cache_type
  {
    container_statement_cache_type (oracle::connection&)
    {
    }
  };

  const char access::object_traits< ::UniPAX::SBase >::persist_statement[] =
  "INSERT INTO \"SBase\" ("
  "\"unipaxId\","
  "\"unipaxSource\","
  "\"metaId\","
  "\"sboTerm\","
  "\"notes\","
  "\"annotation\")"
  " VALUES (:1,:2,:3,:4,:5,:6)";

  const char access::object_traits< ::UniPAX::SBase >::find_statement[] =
  "SELECT "
  "\"SBase\".\"unipaxId\","
  "\"SBase\".\"unipaxSource\","
  "\"SBase\".\"metaId\","
  "\"SBase\".\"sboTerm\","
  "\"SBase\".\"notes\","
  "\"SBase\".\"annotation\""
  " FROM \"SBase\""
  " WHERE \"SBase\".\"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::SBase >::update_statement[] =
  "UPDATE \"SBase\" SET "
  "\"unipaxSource\"=:1,"
  "\"metaId\"=:2,"
  "\"sboTerm\"=:3,"
  "\"notes\"=:4,"
  "\"annotation\"=:5"
  " WHERE \"unipaxId\"=:6";

  const char access::object_traits< ::UniPAX::SBase >::erase_statement[] =
  "DELETE FROM \"SBase\""
  " WHERE \"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::SBase >::query_statement[] =
  "SELECT "
  "\"SBase\".\"unipaxId\","
  "\"SBase\".\"unipaxSource\","
  "\"SBase\".\"metaId\","
  "\"SBase\".\"sboTerm\","
  "\"SBase\".\"notes\","
  "\"SBase\".\"annotation\""
  " FROM \"SBase\""
  " LEFT JOIN \"ImportSource\" \"unipaxSource\" ON \"unipaxSource\".\"unipaxId\" = \"SBase\".\"unipaxSource\""
  " ";

  const char access::object_traits< ::UniPAX::SBase >::erase_query_statement[] =
  "DELETE FROM \"SBase\""
  " ";

  const char access::object_traits< ::UniPAX::SBase >::table_name[] =
  "\"SBase\"";

  void access::object_traits< ::UniPAX::SBase >::
  persist (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();
  }

  void access::object_traits< ::UniPAX::SBase >::
  update (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.update_id_image_version () || idb.version == 0)
    {
      if (i.version != sts.id_image_version () || idb.version == 0)
      {
        bind (idb.bind, i);
        sts.id_image_version (i.version);
        idb.version++;
      }

      sts.update_id_image_version (i.version);

      if (!u)
        imb.version++;
    }

    if (sts.update_statement ().execute () == 0)
      throw object_not_persistent ();
  }

  void access::object_traits< ::UniPAX::SBase >::
  erase (database&, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();
  }

  access::object_traits< ::UniPAX::SBase >::pointer_type
  access::object_traits< ::UniPAX::SBase >::
  find (database& db, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    object_statements< object_type >::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory< object_type, pointer_type  >::create ());
    pointer_traits< pointer_type >::guard pg (p);
    pointer_cache_traits< pointer_type >::insert_guard ig (
      pointer_cache_traits< pointer_type >::insert (db, id, p));
    object_type& obj (pointer_traits< pointer_type >::get_ref (p));

    if (l.locked ())
    {
      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), db);
      sts.find_statement ().stream_result ();
      load_ (sts, obj);
      sts.load_delayed ();
      l.unlock ();
      callback (db, obj, callback_event::post_load);
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits< ::UniPAX::SBase >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, id))
      return false;

    reference_cache_traits< object_type >::insert_guard ig (
      reference_cache_traits< object_type >::insert (db, id, obj));

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    ig.release ();
    return true;
  }

  bool access::object_traits< ::UniPAX::SBase >::
  reload (database& db, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, obj.unipaxId))
      return false;

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits< ::UniPAX::SBase >::
  find_ (oracle::object_statements< object_type >& sts, const id_type& id)
  {
    using namespace oracle;

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());
    st.execute ();
    select_statement::result r (st.fetch ());
    st.free_result ();
    return r != select_statement::no_data;
  }

  result< access::object_traits< ::UniPAX::SBase >::object_type >
  access::object_traits< ::UniPAX::SBase >::
  query (database&, const query_base_type& q)
  {
    using namespace oracle;
    using odb::details::shared;
    using odb::details::shared_ptr;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement + q.clause (),
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) oracle::object_result_impl<object_type> (
        q, st, sts));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits< ::UniPAX::SBase >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    delete_statement st (
      conn,
      erase_query_statement + q.clause (),
      q.parameters_binding ());

    return st.execute ();
  }
}

// Begin epilogue.
//
//
// End epilogue.

#include <odb/post.hxx>
