// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

// Begin prologue.
//
//
// End prologue.

#include <UNIPAX/PERSISTENCE/ORACLE/EXT/ImportSource-odb.hxx>

#include <cassert>
#include <cstring> // std::memcpy

#include <odb/cache-traits.hxx>
#include <odb/details/shared-ptr.hxx>

#include <odb/oracle/binding.hxx>
#include <odb/oracle/traits.hxx>
#include <odb/oracle/database.hxx>
#include <odb/oracle/transaction.hxx>
#include <odb/oracle/connection.hxx>
#include <odb/oracle/statement.hxx>
#include <odb/oracle/statement-cache.hxx>
#include <odb/oracle/object-statements.hxx>
#include <odb/oracle/container-statements.hxx>
#include <odb/oracle/exceptions.hxx>
#include <odb/oracle/result.hxx>

namespace odb
{
  // ImportSource
  //

  const char query_columns_base< ::UniPAX::ImportSource >::successor_alias_[] = "\"successor\"";

  const char query_columns_base< ::UniPAX::ImportSource >::predecessor_alias_[] = "\"predecessor\"";

  const query_columns_base< ::UniPAX::ImportSource >::predecessor_type_
  query_columns_base< ::UniPAX::ImportSource >::predecessor;

  void access::object_traits< ::UniPAX::ImportSource >::
  bind (oracle::bind* b, image_type& i, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    std::size_t n (0);

    // UPBase base
    //
    object_traits< ::UniPAX::UPBase >::bind (b + n, i, sk);
    n += 1UL - (
      sk == statement_select ? 0 : (
        sk == statement_insert ? 0 : 1UL));

    // name
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.name_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.name_value));
    b[n].size = &i.name_size;
    b[n].indicator = &i.name_indicator;
    n++;

    // version
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.version_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.version_value));
    b[n].size = &i.version_size;
    b[n].indicator = &i.version_indicator;
    n++;

    // successor
    //
    b[n].type = oracle::bind::number;
    b[n].buffer = i.successor_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.successor_value));
    b[n].size = &i.successor_size;
    b[n].indicator = &i.successor_indicator;
    n++;

    // predecessor
    //
    if (sk == statement_select)
    {
      b[n].type = oracle::bind::number;
      b[n].buffer = i.predecessor_value;
      b[n].capacity = static_cast<ub4> (sizeof (i.predecessor_value));
      b[n].size = &i.predecessor_size;
      b[n].indicator = &i.predecessor_indicator;
      n++;
    }
  }

  bool access::object_traits< ::UniPAX::ImportSource >::
  init (image_type& i, const object_type& o, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    if (i.change_callback_.callback != 0)
      (i.change_callback_.callback) (i.change_callback_.context);

    bool grew (false);

    // UPBase base
    //
    if (object_traits< ::UniPAX::UPBase >::init (i, o, sk))
      grew = true;

    // name
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.name_value,
        sizeof (i.name_value),
        size,
        is_null,
        o.name);
      i.name_size = static_cast<ub2> (size);
      i.name_indicator = is_null ? -1 : 0;
    }

    // version
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.version_value,
        sizeof (i.version_value),
        size,
        is_null,
        o.version);
      i.version_size = static_cast<ub2> (size);
      i.version_indicator = is_null ? -1 : 0;
    }

    // successor
    //
    {
      typedef object_traits< ::UniPAX::ImportSource > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::ImportSource > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (o.successor));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (o.successor)));

        std::size_t size (0);
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_image (
          i.successor_value,
          size,
          is_null,
          id);
        i.successor_size = static_cast<ub2> (size);
      }

      i.successor_indicator = is_null ? -1 : 0;
    }

    return grew;
  }

  void access::object_traits< ::UniPAX::ImportSource >::
  init (object_type& o, const image_type& i, database& db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // UPBase base
    //
    object_traits< ::UniPAX::UPBase >::init (o, i, db);

    // name
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.name,
      i.name_value,
      i.name_size,
      i.name_indicator == -1);

    // version
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.version,
      i.version_value,
      i.version_size,
      i.version_indicator == -1);

    // successor
    //
    {
      typedef object_traits< ::UniPAX::ImportSource > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::ImportSource > > ptr_traits;

      if (i.successor_indicator == -1)
        o.successor = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_value (
          id,
          i.successor_value,
          i.successor_size,
          i.successor_indicator == -1);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        o.successor = ptr_traits::pointer_type (
          db.load< obj_traits::object_type > (id));
      }
    }

    // predecessor
    //
    {
      typedef object_traits< ::UniPAX::ImportSource > obj_traits;
      typedef pointer_traits< ::boost::weak_ptr< ::UniPAX::ImportSource > > ptr_traits;

      if (i.predecessor_indicator == -1)
        o.predecessor = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_value (
          id,
          i.predecessor_value,
          i.predecessor_size,
          i.predecessor_indicator == -1);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        o.predecessor = ptr_traits::pointer_type (
          db.load< obj_traits::object_type > (id));
      }
    }
  }

  struct access::object_traits< ::UniPAX::ImportSource >::container_statement_cache_type
  {
    container_statement_cache_type (oracle::connection&)
    {
    }
  };

  const char access::object_traits< ::UniPAX::ImportSource >::persist_statement[] =
  "INSERT INTO \"ImportSource\" ("
  "\"unipaxId\","
  "\"name\","
  "\"version\","
  "\"successor\")"
  " VALUES (:1,:2,:3,:4)";

  const char access::object_traits< ::UniPAX::ImportSource >::find_statement[] =
  "SELECT "
  "\"ImportSource\".\"unipaxId\","
  "\"ImportSource\".\"name\","
  "\"ImportSource\".\"version\","
  "\"ImportSource\".\"successor\","
  "\"predecessor\".\"unipaxId\""
  " FROM \"ImportSource\""
  " LEFT JOIN \"ImportSource\" \"predecessor\" ON \"predecessor\".\"successor\" = \"ImportSource\".\"unipaxId\""
  " WHERE \"ImportSource\".\"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::ImportSource >::update_statement[] =
  "UPDATE \"ImportSource\" SET "
  "\"name\"=:1,"
  "\"version\"=:2,"
  "\"successor\"=:3"
  " WHERE \"unipaxId\"=:4";

  const char access::object_traits< ::UniPAX::ImportSource >::erase_statement[] =
  "DELETE FROM \"ImportSource\""
  " WHERE \"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::ImportSource >::query_statement[] =
  "SELECT "
  "\"ImportSource\".\"unipaxId\","
  "\"ImportSource\".\"name\","
  "\"ImportSource\".\"version\","
  "\"ImportSource\".\"successor\","
  "\"predecessor\".\"unipaxId\""
  " FROM \"ImportSource\""
  " LEFT JOIN \"ImportSource\" \"successor\" ON \"successor\".\"unipaxId\" = \"ImportSource\".\"successor\""
  " LEFT JOIN \"ImportSource\" \"predecessor\" ON \"predecessor\".\"successor\" = \"ImportSource\".\"unipaxId\""
  " ";

  const char access::object_traits< ::UniPAX::ImportSource >::erase_query_statement[] =
  "DELETE FROM \"ImportSource\""
  " ";

  const char access::object_traits< ::UniPAX::ImportSource >::table_name[] =
  "\"ImportSource\"";

  void access::object_traits< ::UniPAX::ImportSource >::
  persist (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();
  }

  void access::object_traits< ::UniPAX::ImportSource >::
  update (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.update_id_image_version () || idb.version == 0)
    {
      if (i.version != sts.id_image_version () || idb.version == 0)
      {
        bind (idb.bind, i);
        sts.id_image_version (i.version);
        idb.version++;
      }

      sts.update_id_image_version (i.version);

      if (!u)
        imb.version++;
    }

    if (sts.update_statement ().execute () == 0)
      throw object_not_persistent ();
  }

  void access::object_traits< ::UniPAX::ImportSource >::
  erase (database&, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();
  }

  access::object_traits< ::UniPAX::ImportSource >::pointer_type
  access::object_traits< ::UniPAX::ImportSource >::
  find (database& db, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    object_statements< object_type >::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory< object_type, pointer_type  >::create ());
    pointer_traits< pointer_type >::guard pg (p);
    pointer_cache_traits< pointer_type >::insert_guard ig (
      pointer_cache_traits< pointer_type >::insert (db, id, p));
    object_type& obj (pointer_traits< pointer_type >::get_ref (p));

    if (l.locked ())
    {
      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), db);
      sts.find_statement ().stream_result ();
      load_ (sts, obj);
      sts.load_delayed ();
      l.unlock ();
      callback (db, obj, callback_event::post_load);
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits< ::UniPAX::ImportSource >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, id))
      return false;

    reference_cache_traits< object_type >::insert_guard ig (
      reference_cache_traits< object_type >::insert (db, id, obj));

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    ig.release ();
    return true;
  }

  bool access::object_traits< ::UniPAX::ImportSource >::
  reload (database& db, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, obj.unipaxId))
      return false;

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits< ::UniPAX::ImportSource >::
  find_ (oracle::object_statements< object_type >& sts, const id_type& id)
  {
    using namespace oracle;

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());
    st.execute ();
    select_statement::result r (st.fetch ());
    st.free_result ();
    return r != select_statement::no_data;
  }

  result< access::object_traits< ::UniPAX::ImportSource >::object_type >
  access::object_traits< ::UniPAX::ImportSource >::
  query (database&, const query_base_type& q)
  {
    using namespace oracle;
    using odb::details::shared;
    using odb::details::shared_ptr;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement + q.clause (),
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) oracle::object_result_impl<object_type> (
        q, st, sts));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits< ::UniPAX::ImportSource >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    delete_statement st (
      conn,
      erase_query_statement + q.clause (),
      q.parameters_binding ());

    return st.execute ();
  }
}

// Begin epilogue.
//
//
// End epilogue.

#include <odb/post.hxx>
