// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

// Begin prologue.
//
//
// End prologue.

#include <UNIPAX/PERSISTENCE/ORACLE/EXT/Sample-odb.hxx>

#include <cassert>
#include <cstring> // std::memcpy

#include <odb/cache-traits.hxx>
#include <odb/details/shared-ptr.hxx>

#include <odb/oracle/binding.hxx>
#include <odb/oracle/traits.hxx>
#include <odb/oracle/database.hxx>
#include <odb/oracle/transaction.hxx>
#include <odb/oracle/connection.hxx>
#include <odb/oracle/statement.hxx>
#include <odb/oracle/statement-cache.hxx>
#include <odb/oracle/object-statements.hxx>
#include <odb/oracle/container-statements.hxx>
#include <odb/oracle/exceptions.hxx>
#include <odb/oracle/result.hxx>

namespace odb
{
  // Sample
  //

  void access::object_traits< ::UniPAX::Sample >::
  bind (oracle::bind* b, image_type& i, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    std::size_t n (0);

    // UPBase base
    //
    object_traits< ::UniPAX::UPBase >::bind (b + n, i, sk);
    n += 1UL - (
      sk == statement_select ? 0 : (
        sk == statement_insert ? 0 : 1UL));

    // indexInExperiment
    //
    b[n].type = oracle::bind::integer;
    b[n].buffer = &i.indexInExperiment_value;
    b[n].capacity = 4;
    b[n].size = 0;
    b[n].indicator = &i.indexInExperiment_indicator;
    n++;

    // name
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.name_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.name_value));
    b[n].size = &i.name_size;
    b[n].indicator = &i.name_indicator;
    n++;

    // description
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.description_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.description_value));
    b[n].size = &i.description_size;
    b[n].indicator = &i.description_indicator;
    n++;

    // user
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.user_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.user_value));
    b[n].size = &i.user_size;
    b[n].indicator = &i.user_indicator;
    n++;

    // metaInfos
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.metaInfos_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.metaInfos_value));
    b[n].size = &i.metaInfos_size;
    b[n].indicator = &i.metaInfos_indicator;
    n++;

    // dataType
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.dataType_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.dataType_value));
    b[n].size = &i.dataType_size;
    b[n].indicator = &i.dataType_indicator;
    n++;

    // data
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.data_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.data_value));
    b[n].size = &i.data_size;
    b[n].indicator = &i.data_indicator;
    n++;
  }

  bool access::object_traits< ::UniPAX::Sample >::
  init (image_type& i, const object_type& o, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    if (i.change_callback_.callback != 0)
      (i.change_callback_.callback) (i.change_callback_.context);

    bool grew (false);

    // UPBase base
    //
    if (object_traits< ::UniPAX::UPBase >::init (i, o, sk))
      grew = true;

    // indexInExperiment
    //
    {
      bool is_null;
      oracle::value_traits<
          int,
          oracle::id_int32 >::set_image (
        i.indexInExperiment_value, is_null, o.indexInExperiment);
      i.indexInExperiment_indicator = is_null ? -1 : 0;
    }

    // name
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.name_value,
        sizeof (i.name_value),
        size,
        is_null,
        o.name);
      i.name_size = static_cast<ub2> (size);
      i.name_indicator = is_null ? -1 : 0;
    }

    // description
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.description_value,
        sizeof (i.description_value),
        size,
        is_null,
        o.description);
      i.description_size = static_cast<ub2> (size);
      i.description_indicator = is_null ? -1 : 0;
    }

    // user
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.user_value,
        sizeof (i.user_value),
        size,
        is_null,
        o.user);
      i.user_size = static_cast<ub2> (size);
      i.user_indicator = is_null ? -1 : 0;
    }

    // metaInfos
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.metaInfos_value,
        sizeof (i.metaInfos_value),
        size,
        is_null,
        o.metaInfos);
      i.metaInfos_size = static_cast<ub2> (size);
      i.metaInfos_indicator = is_null ? -1 : 0;
    }

    // dataType
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.dataType_value,
        sizeof (i.dataType_value),
        size,
        is_null,
        o.dataType);
      i.dataType_size = static_cast<ub2> (size);
      i.dataType_indicator = is_null ? -1 : 0;
    }

    // data
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.data_value,
        sizeof (i.data_value),
        size,
        is_null,
        o.data);
      i.data_size = static_cast<ub2> (size);
      i.data_indicator = is_null ? -1 : 0;
    }

    return grew;
  }

  void access::object_traits< ::UniPAX::Sample >::
  init (object_type& o, const image_type& i, database& db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // UPBase base
    //
    object_traits< ::UniPAX::UPBase >::init (o, i, db);

    // indexInExperiment
    //
    oracle::value_traits<
        int,
        oracle::id_int32 >::set_value (
      o.indexInExperiment,
      i.indexInExperiment_value,
      i.indexInExperiment_indicator == -1);

    // name
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.name,
      i.name_value,
      i.name_size,
      i.name_indicator == -1);

    // description
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.description,
      i.description_value,
      i.description_size,
      i.description_indicator == -1);

    // user
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.user,
      i.user_value,
      i.user_size,
      i.user_indicator == -1);

    // metaInfos
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.metaInfos,
      i.metaInfos_value,
      i.metaInfos_size,
      i.metaInfos_indicator == -1);

    // dataType
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.dataType,
      i.dataType_value,
      i.dataType_size,
      i.dataType_indicator == -1);

    // data
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.data,
      i.data_value,
      i.data_size,
      i.data_indicator == -1);
  }

  struct access::object_traits< ::UniPAX::Sample >::container_statement_cache_type
  {
    container_statement_cache_type (oracle::connection&)
    {
    }
  };

  const char access::object_traits< ::UniPAX::Sample >::persist_statement[] =
  "INSERT INTO \"Sample\" ("
  "\"unipaxId\","
  "\"indexInExperiment\","
  "\"name\","
  "\"description\","
  "\"user\","
  "\"metaInfos\","
  "\"dataType\","
  "\"data\")"
  " VALUES (:1,:2,:3,:4,:5,:6,:7,:8)";

  const char access::object_traits< ::UniPAX::Sample >::find_statement[] =
  "SELECT "
  "\"Sample\".\"unipaxId\","
  "\"Sample\".\"indexInExperiment\","
  "\"Sample\".\"name\","
  "\"Sample\".\"description\","
  "\"Sample\".\"user\","
  "\"Sample\".\"metaInfos\","
  "\"Sample\".\"dataType\","
  "\"Sample\".\"data\""
  " FROM \"Sample\""
  " WHERE \"Sample\".\"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::Sample >::update_statement[] =
  "UPDATE \"Sample\" SET "
  "\"indexInExperiment\"=:1,"
  "\"name\"=:2,"
  "\"description\"=:3,"
  "\"user\"=:4,"
  "\"metaInfos\"=:5,"
  "\"dataType\"=:6,"
  "\"data\"=:7"
  " WHERE \"unipaxId\"=:8";

  const char access::object_traits< ::UniPAX::Sample >::erase_statement[] =
  "DELETE FROM \"Sample\""
  " WHERE \"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::Sample >::query_statement[] =
  "SELECT "
  "\"Sample\".\"unipaxId\","
  "\"Sample\".\"indexInExperiment\","
  "\"Sample\".\"name\","
  "\"Sample\".\"description\","
  "\"Sample\".\"user\","
  "\"Sample\".\"metaInfos\","
  "\"Sample\".\"dataType\","
  "\"Sample\".\"data\""
  " FROM \"Sample\""
  " ";

  const char access::object_traits< ::UniPAX::Sample >::erase_query_statement[] =
  "DELETE FROM \"Sample\""
  " ";

  const char access::object_traits< ::UniPAX::Sample >::table_name[] =
  "\"Sample\"";

  void access::object_traits< ::UniPAX::Sample >::
  persist (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();
  }

  void access::object_traits< ::UniPAX::Sample >::
  update (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.update_id_image_version () || idb.version == 0)
    {
      if (i.version != sts.id_image_version () || idb.version == 0)
      {
        bind (idb.bind, i);
        sts.id_image_version (i.version);
        idb.version++;
      }

      sts.update_id_image_version (i.version);

      if (!u)
        imb.version++;
    }

    if (sts.update_statement ().execute () == 0)
      throw object_not_persistent ();
  }

  void access::object_traits< ::UniPAX::Sample >::
  erase (database&, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();
  }

  access::object_traits< ::UniPAX::Sample >::pointer_type
  access::object_traits< ::UniPAX::Sample >::
  find (database& db, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    object_statements< object_type >::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory< object_type, pointer_type  >::create ());
    pointer_traits< pointer_type >::guard pg (p);
    pointer_cache_traits< pointer_type >::insert_guard ig (
      pointer_cache_traits< pointer_type >::insert (db, id, p));
    object_type& obj (pointer_traits< pointer_type >::get_ref (p));

    if (l.locked ())
    {
      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), db);
      sts.find_statement ().stream_result ();
      load_ (sts, obj);
      sts.load_delayed ();
      l.unlock ();
      callback (db, obj, callback_event::post_load);
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits< ::UniPAX::Sample >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, id))
      return false;

    reference_cache_traits< object_type >::insert_guard ig (
      reference_cache_traits< object_type >::insert (db, id, obj));

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    ig.release ();
    return true;
  }

  bool access::object_traits< ::UniPAX::Sample >::
  reload (database& db, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, obj.unipaxId))
      return false;

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits< ::UniPAX::Sample >::
  find_ (oracle::object_statements< object_type >& sts, const id_type& id)
  {
    using namespace oracle;

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());
    st.execute ();
    select_statement::result r (st.fetch ());
    st.free_result ();
    return r != select_statement::no_data;
  }

  result< access::object_traits< ::UniPAX::Sample >::object_type >
  access::object_traits< ::UniPAX::Sample >::
  query (database&, const query_base_type& q)
  {
    using namespace oracle;
    using odb::details::shared;
    using odb::details::shared_ptr;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement + q.clause (),
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) oracle::object_result_impl<object_type> (
        q, st, sts));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits< ::UniPAX::Sample >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    delete_statement st (
      conn,
      erase_query_statement + q.clause (),
      q.parameters_binding ());

    return st.execute ();
  }
}

// Begin epilogue.
//
//
// End epilogue.

#include <odb/post.hxx>
