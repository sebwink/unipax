// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

// Begin prologue.
//
//
// End prologue.

#include <UNIPAX/PERSISTENCE/ORACLE/BIOPAX/SmallMoleculeReference-odb.hxx>

#include <cassert>
#include <cstring> // std::memcpy

#include <odb/cache-traits.hxx>
#include <odb/details/shared-ptr.hxx>

#include <odb/oracle/binding.hxx>
#include <odb/oracle/traits.hxx>
#include <odb/oracle/database.hxx>
#include <odb/oracle/transaction.hxx>
#include <odb/oracle/connection.hxx>
#include <odb/oracle/statement.hxx>
#include <odb/oracle/statement-cache.hxx>
#include <odb/oracle/object-statements.hxx>
#include <odb/oracle/container-statements.hxx>
#include <odb/oracle/exceptions.hxx>
#include <odb/oracle/result.hxx>

namespace odb
{
  // SmallMoleculeReference
  //

  const char query_columns_base< ::UniPAX::SmallMoleculeReference >::structure_alias_[] = "\"structure\"";

  // comment
  //

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::comment_traits::select_all_statement[] =
  "SELECT "
  "\"SmallMoleculeReference_comment\".\"index\","
  "\"SmallMoleculeReference_comment\".\"value\""
  " FROM \"SmallMoleculeReference_comment\" WHERE \"SmallMoleculeReference_comment\".\"object_id\"=:1"
  " ORDER BY \"SmallMoleculeReference_comment\".\"index\""
  ;

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::comment_traits::insert_one_statement[] =
  "INSERT INTO \"SmallMoleculeReference_comment\" ("
  "\"object_id\","
  "\"index\","
  "\"value\")"
  " VALUES (:1,:2,:3)";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::comment_traits::delete_all_statement[] =
  "DELETE FROM \"SmallMoleculeReference_comment\""
  " WHERE \"object_id\"=:1";

  // id_memberEntityReferences
  //

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::id_memberEntityReferences_traits::select_all_statement[] =
  "SELECT "
  "\"SmallMoleculeReference_id_memberEnti\".\"index\","
  "\"SmallMoleculeReference_id_memberEnti\".\"value\""
  " FROM \"SmallMoleculeReference_id_memberEnti\" WHERE \"SmallMoleculeReference_id_memberEnti\".\"object_id\"=:1"
  " ORDER BY \"SmallMoleculeReference_id_memberEnti\".\"index\""
  ;

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::id_memberEntityReferences_traits::insert_one_statement[] =
  "INSERT INTO \"SmallMoleculeReference_id_memberEnti\" ("
  "\"object_id\","
  "\"index\","
  "\"value\")"
  " VALUES (:1,:2,:3)";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::id_memberEntityReferences_traits::delete_all_statement[] =
  "DELETE FROM \"SmallMoleculeReference_id_memberEnti\""
  " WHERE \"object_id\"=:1";

  // evidence
  //

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::evidence_traits::select_all_statement[] =
  "SELECT "
  "\"SmallMoleculeReference_evidence\".\"index\","
  "\"SmallMoleculeReference_evidence\".\"value\""
  " FROM \"SmallMoleculeReference_evidence\" WHERE \"SmallMoleculeReference_evidence\".\"object_id\"=:1"
  " ORDER BY \"SmallMoleculeReference_evidence\".\"index\""
  ;

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::evidence_traits::insert_one_statement[] =
  "INSERT INTO \"SmallMoleculeReference_evidence\" ("
  "\"object_id\","
  "\"index\","
  "\"value\")"
  " VALUES (:1,:2,:3)";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::evidence_traits::delete_all_statement[] =
  "DELETE FROM \"SmallMoleculeReference_evidence\""
  " WHERE \"object_id\"=:1";

  // id_xref
  //

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::id_xref_traits::select_all_statement[] =
  "SELECT "
  "\"SmallMoleculeReference_id_xref\".\"index\","
  "\"SmallMoleculeReference_id_xref\".\"value\""
  " FROM \"SmallMoleculeReference_id_xref\" WHERE \"SmallMoleculeReference_id_xref\".\"object_id\"=:1"
  " ORDER BY \"SmallMoleculeReference_id_xref\".\"index\""
  ;

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::id_xref_traits::insert_one_statement[] =
  "INSERT INTO \"SmallMoleculeReference_id_xref\" ("
  "\"object_id\","
  "\"index\","
  "\"value\")"
  " VALUES (:1,:2,:3)";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::id_xref_traits::delete_all_statement[] =
  "DELETE FROM \"SmallMoleculeReference_id_xref\""
  " WHERE \"object_id\"=:1";

  // name
  //

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::name_traits::select_all_statement[] =
  "SELECT "
  "\"SmallMoleculeReference_name\".\"index\","
  "\"SmallMoleculeReference_name\".\"value\""
  " FROM \"SmallMoleculeReference_name\" WHERE \"SmallMoleculeReference_name\".\"object_id\"=:1"
  " ORDER BY \"SmallMoleculeReference_name\".\"index\""
  ;

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::name_traits::insert_one_statement[] =
  "INSERT INTO \"SmallMoleculeReference_name\" ("
  "\"object_id\","
  "\"index\","
  "\"value\")"
  " VALUES (:1,:2,:3)";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::name_traits::delete_all_statement[] =
  "DELETE FROM \"SmallMoleculeReference_name\""
  " WHERE \"object_id\"=:1";

  void access::object_traits< ::UniPAX::SmallMoleculeReference >::
  bind (oracle::bind* b, image_type& i, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    std::size_t n (0);

    // EntityReference base
    //
    object_traits< ::UniPAX::EntityReference >::bind (b + n, i, sk);
    n += 6UL - (
      sk == statement_select ? 0 : (
        sk == statement_insert ? 0 : 1UL));

    // chemicalFormula
    //
    b[n].type = oracle::bind::string;
    b[n].buffer = i.chemicalFormula_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.chemicalFormula_value));
    b[n].size = &i.chemicalFormula_size;
    b[n].indicator = &i.chemicalFormula_indicator;
    n++;

    // molecularWeight
    //
    b[n].type = oracle::bind::binary_double;
    b[n].buffer= &i.molecularWeight_value;
    b[n].capacity = 8;
    b[n].size = 0;
    b[n].indicator = &i.molecularWeight_indicator;
    n++;

    // structure
    //
    b[n].type = oracle::bind::number;
    b[n].buffer = i.structure_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.structure_value));
    b[n].size = &i.structure_size;
    b[n].indicator = &i.structure_indicator;
    n++;
  }

  bool access::object_traits< ::UniPAX::SmallMoleculeReference >::
  init (image_type& i, const object_type& o, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    if (i.change_callback_.callback != 0)
      (i.change_callback_.callback) (i.change_callback_.context);

    bool grew (false);

    // EntityReference base
    //
    if (object_traits< ::UniPAX::EntityReference >::init (i, o, sk))
      grew = true;

    // chemicalFormula
    //
    {
      bool is_null;
      std::size_t size (0);
      oracle::value_traits<
          ::std::string,
          oracle::id_string >::set_image (
        i.chemicalFormula_value,
        sizeof (i.chemicalFormula_value),
        size,
        is_null,
        o.chemicalFormula);
      i.chemicalFormula_size = static_cast<ub2> (size);
      i.chemicalFormula_indicator = is_null ? -1 : 0;
    }

    // molecularWeight
    //
    {
      bool is_null;
      oracle::value_traits<
          double,
          oracle::id_double >::set_image (
        i.molecularWeight_value, is_null, o.molecularWeight);
      i.molecularWeight_indicator = is_null ? -1 : 0;
    }

    // structure
    //
    {
      typedef object_traits< ::UniPAX::ChemicalStructure > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::ChemicalStructure > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (o.structure));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (o.structure)));

        std::size_t size (0);
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_image (
          i.structure_value,
          size,
          is_null,
          id);
        i.structure_size = static_cast<ub2> (size);
      }

      i.structure_indicator = is_null ? -1 : 0;
    }

    return grew;
  }

  void access::object_traits< ::UniPAX::SmallMoleculeReference >::
  init (object_type& o, const image_type& i, database& db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // EntityReference base
    //
    object_traits< ::UniPAX::EntityReference >::init (o, i, db);

    // chemicalFormula
    //
    oracle::value_traits<
        ::std::string,
        oracle::id_string >::set_value (
      o.chemicalFormula,
      i.chemicalFormula_value,
      i.chemicalFormula_size,
      i.chemicalFormula_indicator == -1);

    // molecularWeight
    //
    oracle::value_traits<
        double,
        oracle::id_double >::set_value (
      o.molecularWeight,
      i.molecularWeight_value,
      i.molecularWeight_indicator == -1);

    // structure
    //
    {
      typedef object_traits< ::UniPAX::ChemicalStructure > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::ChemicalStructure > > ptr_traits;

      if (i.structure_indicator == -1)
        o.structure = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_value (
          id,
          i.structure_value,
          i.structure_size,
          i.structure_indicator == -1);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        o.structure = ptr_traits::pointer_type (
          db.load< obj_traits::object_type > (id));
      }
    }
  }

  struct access::object_traits< ::UniPAX::SmallMoleculeReference >::container_statement_cache_type
  {
    oracle::container_statements_impl< comment_traits > comment;
    oracle::container_statements_impl< id_memberEntityReferences_traits > id_memberEntityReferences;
    oracle::container_statements_impl< evidence_traits > evidence;
    oracle::container_statements_impl< id_xref_traits > id_xref;
    oracle::container_statements_impl< name_traits > name;

    container_statement_cache_type (oracle::connection& c)
    : comment (c),
      id_memberEntityReferences (c),
      evidence (c),
      id_xref (c),
      name (c)
    {
    }
  };

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::persist_statement[] =
  "INSERT INTO \"SmallMoleculeReference" ("
  "\"unipaxId\","
  "\"unipaxSource\","
  "\"entityReferenceType\","
  "\"id_entityFeature\","
  "\"standardName\","
  "\"displayName\","
  "\"chemicalFormula\","
  "\"molecularWeight\","
  "\"structure\")"
  " VALUES (:1,:2,:3,:4,:5,:6,:7,:8,:9)";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::find_statement[] =
  "SELECT "
  "\"SmallMoleculeReference".\"unipaxId\","
  "\"SmallMoleculeReference".\"unipaxSource\","
  "\"SmallMoleculeReference".\"entityReferenceType\","
  "\"SmallMoleculeReference".\"id_entityFeature\","
  "\"SmallMoleculeReference".\"standardName\","
  "\"SmallMoleculeReference".\"displayName\","
  "\"SmallMoleculeReference".\"chemicalFormula\","
  "\"SmallMoleculeReference".\"molecularWeight\","
  "\"SmallMoleculeReference".\"structure\""
  " FROM \"SmallMoleculeReference""
  " WHERE \"SmallMoleculeReference".\"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::update_statement[] =
  "UPDATE \"SmallMoleculeReference" SET "
  "\"unipaxSource\"=:1,"
  "\"entityReferenceType\"=:2,"
  "\"id_entityFeature\"=:3,"
  "\"standardName\"=:4,"
  "\"displayName\"=:5,"
  "\"chemicalFormula\"=:6,"
  "\"molecularWeight\"=:7,"
  "\"structure\"=:8"
  " WHERE \"unipaxId\"=:9";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::erase_statement[] =
  "DELETE FROM \"SmallMoleculeReference""
  " WHERE \"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::query_statement[] =
  "SELECT "
  "\"SmallMoleculeReference".\"unipaxId\","
  "\"SmallMoleculeReference".\"unipaxSource\","
  "\"SmallMoleculeReference".\"entityReferenceType\","
  "\"SmallMoleculeReference".\"id_entityFeature\","
  "\"SmallMoleculeReference".\"standardName\","
  "\"SmallMoleculeReference".\"displayName\","
  "\"SmallMoleculeReference".\"chemicalFormula\","
  "\"SmallMoleculeReference".\"molecularWeight\","
  "\"SmallMoleculeReference".\"structure\""
  " FROM \"SmallMoleculeReference""
  " LEFT JOIN \"ImportSource\" \"unipaxSource\" ON \"unipaxSource\".\"unipaxId\" = \"SmallMoleculeReference".\"unipaxSource\""
  " LEFT JOIN \"EntityReferenceTypeVocabulary\" \"entityReferenceType\" ON \"entityReferenceType\".\"unipaxId\" = \"SmallMoleculeReference".\"entityReferenceType\""
  " LEFT JOIN \"ChemicalStructure\" \"structure\" ON \"structure\".\"unipaxId\" = \"SmallMoleculeReference".\"structure\""
  " ";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::erase_query_statement[] =
  "DELETE FROM \"SmallMoleculeReference""
  " ";

  const char access::object_traits< ::UniPAX::SmallMoleculeReference >::table_name[] =
  "\"SmallMoleculeReference"";

  void access::object_traits< ::UniPAX::SmallMoleculeReference >::
  persist (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    comment_traits::persist (
      obj.comment,
      idb,
      sts.container_statment_cache ().comment);

    id_memberEntityReferences_traits::persist (
      obj.id_memberEntityReferences,
      idb,
      sts.container_statment_cache ().id_memberEntityReferences);

    evidence_traits::persist (
      obj.evidence,
      idb,
      sts.container_statment_cache ().evidence);

    id_xref_traits::persist (
      obj.id_xref,
      idb,
      sts.container_statment_cache ().id_xref);

    name_traits::persist (
      obj.name,
      idb,
      sts.container_statment_cache ().name);
  }

  void access::object_traits< ::UniPAX::SmallMoleculeReference >::
  update (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.update_id_image_version () || idb.version == 0)
    {
      if (i.version != sts.id_image_version () || idb.version == 0)
      {
        bind (idb.bind, i);
        sts.id_image_version (i.version);
        idb.version++;
      }

      sts.update_id_image_version (i.version);

      if (!u)
        imb.version++;
    }

    if (sts.update_statement ().execute () == 0)
      throw object_not_persistent ();

    comment_traits::update (
      obj.comment,
      idb,
      sts.container_statment_cache ().comment);

    id_memberEntityReferences_traits::update (
      obj.id_memberEntityReferences,
      idb,
      sts.container_statment_cache ().id_memberEntityReferences);

    evidence_traits::update (
      obj.evidence,
      idb,
      sts.container_statment_cache ().evidence);

    id_xref_traits::update (
      obj.id_xref,
      idb,
      sts.container_statment_cache ().id_xref);

    name_traits::update (
      obj.name,
      idb,
      sts.container_statment_cache ().name);
  }

  void access::object_traits< ::UniPAX::SmallMoleculeReference >::
  erase (database&, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    comment_traits::erase (
      idb,
      sts.container_statment_cache ().comment);

    id_memberEntityReferences_traits::erase (
      idb,
      sts.container_statment_cache ().id_memberEntityReferences);

    evidence_traits::erase (
      idb,
      sts.container_statment_cache ().evidence);

    id_xref_traits::erase (
      idb,
      sts.container_statment_cache ().id_xref);

    name_traits::erase (
      idb,
      sts.container_statment_cache ().name);

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();
  }

  access::object_traits< ::UniPAX::SmallMoleculeReference >::pointer_type
  access::object_traits< ::UniPAX::SmallMoleculeReference >::
  find (database& db, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    object_statements< object_type >::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory< object_type, pointer_type  >::create ());
    pointer_traits< pointer_type >::guard pg (p);
    pointer_cache_traits< pointer_type >::insert_guard ig (
      pointer_cache_traits< pointer_type >::insert (db, id, p));
    object_type& obj (pointer_traits< pointer_type >::get_ref (p));

    if (l.locked ())
    {
      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), db);
      sts.find_statement ().stream_result ();
      load_ (sts, obj);
      sts.load_delayed ();
      l.unlock ();
      callback (db, obj, callback_event::post_load);
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits< ::UniPAX::SmallMoleculeReference >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, id))
      return false;

    reference_cache_traits< object_type >::insert_guard ig (
      reference_cache_traits< object_type >::insert (db, id, obj));

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    ig.release ();
    return true;
  }

  bool access::object_traits< ::UniPAX::SmallMoleculeReference >::
  reload (database& db, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, obj.unipaxId))
      return false;

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits< ::UniPAX::SmallMoleculeReference >::
  find_ (oracle::object_statements< object_type >& sts, const id_type& id)
  {
    using namespace oracle;

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());
    st.execute ();
    select_statement::result r (st.fetch ());
    st.free_result ();
    return r != select_statement::no_data;
  }

  void access::object_traits< ::UniPAX::SmallMoleculeReference >::
  load_ (oracle::object_statements< object_type >& sts, object_type& obj)
  {
    oracle::binding& idb (sts.id_image_binding ());

    comment_traits::load (
      obj.comment,
      idb,
      sts.container_statment_cache ().comment);

    id_memberEntityReferences_traits::load (
      obj.id_memberEntityReferences,
      idb,
      sts.container_statment_cache ().id_memberEntityReferences);

    evidence_traits::load (
      obj.evidence,
      idb,
      sts.container_statment_cache ().evidence);

    id_xref_traits::load (
      obj.id_xref,
      idb,
      sts.container_statment_cache ().id_xref);

    name_traits::load (
      obj.name,
      idb,
      sts.container_statment_cache ().name);
  }

  result< access::object_traits< ::UniPAX::SmallMoleculeReference >::object_type >
  access::object_traits< ::UniPAX::SmallMoleculeReference >::
  query (database&, const query_base_type& q)
  {
    using namespace oracle;
    using odb::details::shared;
    using odb::details::shared_ptr;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement + q.clause (),
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) oracle::object_result_impl<object_type> (
        q, st, sts));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits< ::UniPAX::SmallMoleculeReference >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    delete_statement st (
      conn,
      erase_query_statement + q.clause (),
      q.parameters_binding ());

    return st.execute ();
  }
}

// Begin epilogue.
//
//
// End epilogue.

#include <odb/post.hxx>
