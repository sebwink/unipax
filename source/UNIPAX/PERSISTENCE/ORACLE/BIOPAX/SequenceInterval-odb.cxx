// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

// Begin prologue.
//
//
// End prologue.

#include <UNIPAX/PERSISTENCE/ORACLE/BIOPAX/SequenceInterval-odb.hxx>

#include <cassert>
#include <cstring> // std::memcpy

#include <odb/cache-traits.hxx>
#include <odb/details/shared-ptr.hxx>

#include <odb/oracle/binding.hxx>
#include <odb/oracle/traits.hxx>
#include <odb/oracle/database.hxx>
#include <odb/oracle/transaction.hxx>
#include <odb/oracle/connection.hxx>
#include <odb/oracle/statement.hxx>
#include <odb/oracle/statement-cache.hxx>
#include <odb/oracle/object-statements.hxx>
#include <odb/oracle/container-statements.hxx>
#include <odb/oracle/exceptions.hxx>
#include <odb/oracle/result.hxx>

namespace odb
{
  // SequenceInterval
  //

  const char query_columns_base< ::UniPAX::SequenceInterval >::sequenceIntervalBegin_alias_[] = "\"sequenceIntervalBegin\"";

  const char query_columns_base< ::UniPAX::SequenceInterval >::sequenceIntervalEnd_alias_[] = "\"sequenceIntervalEnd\"";

  // comment
  //

  const char access::object_traits< ::UniPAX::SequenceInterval >::comment_traits::select_all_statement[] =
  "SELECT "
  "\"SequenceInterval_comment\".\"index\","
  "\"SequenceInterval_comment\".\"value\""
  " FROM \"SequenceInterval_comment\" WHERE \"SequenceInterval_comment\".\"object_id\"=:1"
  " ORDER BY \"SequenceInterval_comment\".\"index\""
  ;

  const char access::object_traits< ::UniPAX::SequenceInterval >::comment_traits::insert_one_statement[] =
  "INSERT INTO \"SequenceInterval_comment\" ("
  "\"object_id\","
  "\"index\","
  "\"value\")"
  " VALUES (:1,:2,:3)";

  const char access::object_traits< ::UniPAX::SequenceInterval >::comment_traits::delete_all_statement[] =
  "DELETE FROM \"SequenceInterval_comment\""
  " WHERE \"object_id\"=:1";

  void access::object_traits< ::UniPAX::SequenceInterval >::
  bind (oracle::bind* b, image_type& i, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    std::size_t n (0);

    // SequenceLocation base
    //
    object_traits< ::UniPAX::SequenceLocation >::bind (b + n, i, sk);
    n += 2UL - (
      sk == statement_select ? 0 : (
        sk == statement_insert ? 0 : 1UL));

    // sequenceIntervalBegin
    //
    b[n].type = oracle::bind::number;
    b[n].buffer = i.sequenceIntervalBegin_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.sequenceIntervalBegin_value));
    b[n].size = &i.sequenceIntervalBegin_size;
    b[n].indicator = &i.sequenceIntervalBegin_indicator;
    n++;

    // sequenceIntervalEnd
    //
    b[n].type = oracle::bind::number;
    b[n].buffer = i.sequenceIntervalEnd_value;
    b[n].capacity = static_cast<ub4> (sizeof (i.sequenceIntervalEnd_value));
    b[n].size = &i.sequenceIntervalEnd_size;
    b[n].indicator = &i.sequenceIntervalEnd_indicator;
    n++;
  }

  bool access::object_traits< ::UniPAX::SequenceInterval >::
  init (image_type& i, const object_type& o, oracle::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace oracle;

    if (i.change_callback_.callback != 0)
      (i.change_callback_.callback) (i.change_callback_.context);

    bool grew (false);

    // SequenceLocation base
    //
    if (object_traits< ::UniPAX::SequenceLocation >::init (i, o, sk))
      grew = true;

    // sequenceIntervalBegin
    //
    {
      typedef object_traits< ::UniPAX::SequenceSite > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::SequenceSite > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (o.sequenceIntervalBegin));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (o.sequenceIntervalBegin)));

        std::size_t size (0);
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_image (
          i.sequenceIntervalBegin_value,
          size,
          is_null,
          id);
        i.sequenceIntervalBegin_size = static_cast<ub2> (size);
      }

      i.sequenceIntervalBegin_indicator = is_null ? -1 : 0;
    }

    // sequenceIntervalEnd
    //
    {
      typedef object_traits< ::UniPAX::SequenceSite > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::SequenceSite > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (o.sequenceIntervalEnd));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (o.sequenceIntervalEnd)));

        std::size_t size (0);
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_image (
          i.sequenceIntervalEnd_value,
          size,
          is_null,
          id);
        i.sequenceIntervalEnd_size = static_cast<ub2> (size);
      }

      i.sequenceIntervalEnd_indicator = is_null ? -1 : 0;
    }

    return grew;
  }

  void access::object_traits< ::UniPAX::SequenceInterval >::
  init (object_type& o, const image_type& i, database& db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // SequenceLocation base
    //
    object_traits< ::UniPAX::SequenceLocation >::init (o, i, db);

    // sequenceIntervalBegin
    //
    {
      typedef object_traits< ::UniPAX::SequenceSite > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::SequenceSite > > ptr_traits;

      if (i.sequenceIntervalBegin_indicator == -1)
        o.sequenceIntervalBegin = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_value (
          id,
          i.sequenceIntervalBegin_value,
          i.sequenceIntervalBegin_size,
          i.sequenceIntervalBegin_indicator == -1);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        o.sequenceIntervalBegin = ptr_traits::pointer_type (
          db.load< obj_traits::object_type > (id));
      }
    }

    // sequenceIntervalEnd
    //
    {
      typedef object_traits< ::UniPAX::SequenceSite > obj_traits;
      typedef pointer_traits< ::boost::shared_ptr< ::UniPAX::SequenceSite > > ptr_traits;

      if (i.sequenceIntervalEnd_indicator == -1)
        o.sequenceIntervalEnd = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        oracle::value_traits<
            obj_traits::id_type,
            oracle::id_big_int >::set_value (
          id,
          i.sequenceIntervalEnd_value,
          i.sequenceIntervalEnd_size,
          i.sequenceIntervalEnd_indicator == -1);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        o.sequenceIntervalEnd = ptr_traits::pointer_type (
          db.load< obj_traits::object_type > (id));
      }
    }
  }

  struct access::object_traits< ::UniPAX::SequenceInterval >::container_statement_cache_type
  {
    oracle::container_statements_impl< comment_traits > comment;

    container_statement_cache_type (oracle::connection& c)
    : comment (c)
    {
    }
  };

  const char access::object_traits< ::UniPAX::SequenceInterval >::persist_statement[] =
  "INSERT INTO \"SequenceInterval\" ("
  "\"unipaxId\","
  "\"unipaxSource\","
  "\"sequenceIntervalBegin\","
  "\"sequenceIntervalEnd\")"
  " VALUES (:1,:2,:3,:4)";

  const char access::object_traits< ::UniPAX::SequenceInterval >::find_statement[] =
  "SELECT "
  "\"SequenceInterval\".\"unipaxId\","
  "\"SequenceInterval\".\"unipaxSource\","
  "\"SequenceInterval\".\"sequenceIntervalBegin\","
  "\"SequenceInterval\".\"sequenceIntervalEnd\""
  " FROM \"SequenceInterval\""
  " WHERE \"SequenceInterval\".\"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::SequenceInterval >::update_statement[] =
  "UPDATE \"SequenceInterval\" SET "
  "\"unipaxSource\"=:1,"
  "\"sequenceIntervalBegin\"=:2,"
  "\"sequenceIntervalEnd\"=:3"
  " WHERE \"unipaxId\"=:4";

  const char access::object_traits< ::UniPAX::SequenceInterval >::erase_statement[] =
  "DELETE FROM \"SequenceInterval\""
  " WHERE \"unipaxId\"=:1";

  const char access::object_traits< ::UniPAX::SequenceInterval >::query_statement[] =
  "SELECT "
  "\"SequenceInterval\".\"unipaxId\","
  "\"SequenceInterval\".\"unipaxSource\","
  "\"SequenceInterval\".\"sequenceIntervalBegin\","
  "\"SequenceInterval\".\"sequenceIntervalEnd\""
  " FROM \"SequenceInterval\""
  " LEFT JOIN \"ImportSource\" \"unipaxSource\" ON \"unipaxSource\".\"unipaxId\" = \"SequenceInterval\".\"unipaxSource\""
  " LEFT JOIN \"SequenceSite\" \"sequenceIntervalBegin\" ON \"sequenceIntervalBegin\".\"unipaxId\" = \"SequenceInterval\".\"sequenceIntervalBegin\""
  " LEFT JOIN \"SequenceSite\" \"sequenceIntervalEnd\" ON \"sequenceIntervalEnd\".\"unipaxId\" = \"SequenceInterval\".\"sequenceIntervalEnd\""
  " ";

  const char access::object_traits< ::UniPAX::SequenceInterval >::erase_query_statement[] =
  "DELETE FROM \"SequenceInterval\""
  " ";

  const char access::object_traits< ::UniPAX::SequenceInterval >::table_name[] =
  "\"SequenceInterval\"";

  void access::object_traits< ::UniPAX::SequenceInterval >::
  persist (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    comment_traits::persist (
      obj.comment,
      idb,
      sts.container_statment_cache ().comment);
  }

  void access::object_traits< ::UniPAX::SequenceInterval >::
  update (database&, const object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.update_id_image_version () || idb.version == 0)
    {
      if (i.version != sts.id_image_version () || idb.version == 0)
      {
        bind (idb.bind, i);
        sts.id_image_version (i.version);
        idb.version++;
      }

      sts.update_id_image_version (i.version);

      if (!u)
        imb.version++;
    }

    if (sts.update_statement ().execute () == 0)
      throw object_not_persistent ();

    comment_traits::update (
      obj.comment,
      idb,
      sts.container_statment_cache ().comment);
  }

  void access::object_traits< ::UniPAX::SequenceInterval >::
  erase (database&, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    comment_traits::erase (
      idb,
      sts.container_statment_cache ().comment);

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();
  }

  access::object_traits< ::UniPAX::SequenceInterval >::pointer_type
  access::object_traits< ::UniPAX::SequenceInterval >::
  find (database& db, const id_type& id)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());
    object_statements< object_type >::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory< object_type, pointer_type  >::create ());
    pointer_traits< pointer_type >::guard pg (p);
    pointer_cache_traits< pointer_type >::insert_guard ig (
      pointer_cache_traits< pointer_type >::insert (db, id, p));
    object_type& obj (pointer_traits< pointer_type >::get_ref (p));

    if (l.locked ())
    {
      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), db);
      sts.find_statement ().stream_result ();
      load_ (sts, obj);
      sts.load_delayed ();
      l.unlock ();
      callback (db, obj, callback_event::post_load);
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits< ::UniPAX::SequenceInterval >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, id))
      return false;

    reference_cache_traits< object_type >::insert_guard ig (
      reference_cache_traits< object_type >::insert (db, id, obj));

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    ig.release ();
    return true;
  }

  bool access::object_traits< ::UniPAX::SequenceInterval >::
  reload (database& db, object_type& obj)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());
    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    object_statements< object_type >::auto_lock l (sts);

    if (!find_ (sts, obj.unipaxId))
      return false;

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), db);
    sts.find_statement ().stream_result ();
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits< ::UniPAX::SequenceInterval >::
  find_ (oracle::object_statements< object_type >& sts, const id_type& id)
  {
    using namespace oracle;

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());
    st.execute ();
    select_statement::result r (st.fetch ());
    st.free_result ();
    return r != select_statement::no_data;
  }

  void access::object_traits< ::UniPAX::SequenceInterval >::
  load_ (oracle::object_statements< object_type >& sts, object_type& obj)
  {
    oracle::binding& idb (sts.id_image_binding ());

    comment_traits::load (
      obj.comment,
      idb,
      sts.container_statment_cache ().comment);
  }

  result< access::object_traits< ::UniPAX::SequenceInterval >::object_type >
  access::object_traits< ::UniPAX::SequenceInterval >::
  query (database&, const query_base_type& q)
  {
    using namespace oracle;
    using odb::details::shared;
    using odb::details::shared_ptr;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    object_statements< object_type >& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () || imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement + q.clause (),
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) oracle::object_result_impl<object_type> (
        q, st, sts));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits< ::UniPAX::SequenceInterval >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace oracle;

    oracle::connection& conn (
      oracle::transaction::current ().connection ());

    delete_statement st (
      conn,
      erase_query_statement + q.clause (),
      q.parameters_binding ());

    return st.execute ();
  }
}

// Begin epilogue.
//
//
// End epilogue.

#include <odb/post.hxx>
