// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include <UNIPAX/PERSISTENCE/MYSQL/BIOPAX/EntityReference-odb.hxx>

#include <cassert>
#include <cstring>  // std::memcpy
#include <typeinfo>

#include <odb/polymorphic-map.hxx>

#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/polymorphic-object-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/polymorphic-object-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // EntityReference
  //

  const char alias_traits<  ::UniPAX::ControlledVocabulary,
    id_mysql,
    access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::entityReferenceType_tag>::
  table_name[] = "`entityReferenceType_ControlledVocabulary`";

  const char alias_traits<  ::UniPAX::EntityReferenceTypeVocabulary,
    id_mysql,
    access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::entityReferenceType_tag>::
  table_name[] = "`entityReferenceType_EntityReferenceTypeVocabulary`";

  const char alias_traits<  ::UniPAX::EntityFeature,
    id_mysql,
    access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::entityFeature_tag>::
  table_name[] = "`entityFeature_EntityFeature`";

  struct access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::extra_statement_cache_type
  {
    mysql::container_statements_impl< comment_traits > comment;
    mysql::container_statements_impl< memberEntityReferences_traits > memberEntityReferences;
    mysql::container_statements_impl< evidence_traits > evidence;
    mysql::container_statements_impl< xref_traits > xref;
    mysql::container_statements_impl< name_traits > name;

    extra_statement_cache_type (
      mysql::connection& c,
      image_type&,
      id_image_type&,
      mysql::binding& id,
      mysql::binding&)
    : comment (c, id),
      memberEntityReferences (c, id),
      evidence (c, id),
      xref (c, id),
      name (c, id)
    {
    }
  };

  // comment
  //

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::comment_traits::
  select_statement[] =
  "SELECT "
  "`EntityReference_comment`.`index`, "
  "`EntityReference_comment`.`value` "
  "FROM `EntityReference_comment` "
  "WHERE `EntityReference_comment`.`object_id`=? ORDER BY `EntityReference_comment`.`index`";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::comment_traits::
  insert_statement[] =
  "INSERT INTO `EntityReference_comment` "
  "(`object_id`, "
  "`index`, "
  "`value`) "
  "VALUES "
  "(?, ?, ?)";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::comment_traits::
  delete_statement[] =
  "DELETE FROM `EntityReference_comment` "
  "WHERE `object_id`=?";

  // memberEntityReferences
  //

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  select_statement[] =
  "SELECT "
  "`EntityReference_memberEntityReferences`.`index`, "
  "`EntityReference_memberEntityReferences`.`value` "
  "FROM `EntityReference_memberEntityReferences` "
  "WHERE `EntityReference_memberEntityReferences`.`object_id`=? ORDER BY `EntityReference_memberEntityReferences`.`index`";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  insert_statement[] =
  "INSERT INTO `EntityReference_memberEntityReferences` "
  "(`object_id`, "
  "`index`, "
  "`value`) "
  "VALUES "
  "(?, ?, ?)";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  delete_statement[] =
  "DELETE FROM `EntityReference_memberEntityReferences` "
  "WHERE `object_id`=?";

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  bind (MYSQL_BIND* b,
        const MYSQL_BIND* id,
        std::size_t id_size,
        data_image_type& d)
  {
    using namespace mysql;

    statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    size_t n (0);

    // object_id
    //
    if (id != 0)
      std::memcpy (&b[n], id, id_size * sizeof (id[0]));
    n += id_size;

    // index
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &d.index_value;
    b[n].is_null = &d.index_null;
    n++;

    // value
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &d.value_value;
    b[n].is_null = &d.value_null;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  grow (data_image_type& i,
        my_bool* t)
  {
    bool grew (false);

    // index
    //
    t[0UL] = 0;

    // value
    //
    t[1UL] = 0;

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  init (data_image_type& i,
        index_type* j,
        const value_type& v)
  {
    using namespace mysql;

    statement_kind sk (statement_insert);
    ODB_POTENTIALLY_UNUSED (sk);

    bool grew (false);

    // index
    //
    if (j != 0)
    {
      bool is_null (false);
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_image (
        i.index_value, is_null, *j);
      i.index_null = is_null;
    }

    // value
    //
    {
      typedef object_traits< ::UniPAX::EntityReference > obj_traits;
      typedef odb::pointer_traits< value_type > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.value_value, is_null, id);
        i.value_null = is_null;
      }
      else
        i.value_null = 1;
    }

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  init (index_type& j,
        value_type& v,
        const data_image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (db);

    // index
    //
    {
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_value (
        j,
        i.index_value,
        i.index_null);
    }

    // value
    //
    {
      typedef object_traits< ::UniPAX::EntityReference > obj_traits;
      typedef odb::pointer_traits< value_type > ptr_traits;

      if (i.value_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.value_value,
          i.value_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  insert (index_type i, const value_type& v, void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (di, &i, v);

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    if (!sts.insert_statement ().execute ())
      throw object_already_persistent ();
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  select (index_type& i, value_type& v, void* d)
  {
    using namespace mysql;
    using mysql::select_statement;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (i, v, di, &sts.connection ().database ());

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    select_statement::result r (st.fetch ());
    return r != select_statement::no_data;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  delete_ (void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    sts.delete_statement ().execute ();
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  persist (const container_type& c,
           statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::persist (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  load (container_type& c,
        statements_type& sts)
  {
    using namespace mysql;
    using mysql::select_statement;

    const binding& id (sts.id_binding ());

    if (sts.data_binding_test_version ())
    {
      bind (sts.data_bind (), id.bind, id.count, sts.data_image ());
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    st.execute ();
    auto_result ar (st);
    st.cache ();
    select_statement::result r (st.fetch ());
    bool more (r != select_statement::no_data);

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::load (c, more, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  update (const container_type& c,
          statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::update (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::memberEntityReferences_traits::
  erase (statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::erase (fs);
  }

  // evidence
  //

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  select_statement[] =
  "SELECT "
  "`EntityReference_evidence`.`index`, "
  "`EntityReference_evidence`.`value` "
  "FROM `EntityReference_evidence` "
  "WHERE `EntityReference_evidence`.`object_id`=? ORDER BY `EntityReference_evidence`.`index`";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  insert_statement[] =
  "INSERT INTO `EntityReference_evidence` "
  "(`object_id`, "
  "`index`, "
  "`value`) "
  "VALUES "
  "(?, ?, ?)";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  delete_statement[] =
  "DELETE FROM `EntityReference_evidence` "
  "WHERE `object_id`=?";

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  bind (MYSQL_BIND* b,
        const MYSQL_BIND* id,
        std::size_t id_size,
        data_image_type& d)
  {
    using namespace mysql;

    statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    size_t n (0);

    // object_id
    //
    if (id != 0)
      std::memcpy (&b[n], id, id_size * sizeof (id[0]));
    n += id_size;

    // index
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &d.index_value;
    b[n].is_null = &d.index_null;
    n++;

    // value
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &d.value_value;
    b[n].is_null = &d.value_null;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  grow (data_image_type& i,
        my_bool* t)
  {
    bool grew (false);

    // index
    //
    t[0UL] = 0;

    // value
    //
    t[1UL] = 0;

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  init (data_image_type& i,
        index_type* j,
        const value_type& v)
  {
    using namespace mysql;

    statement_kind sk (statement_insert);
    ODB_POTENTIALLY_UNUSED (sk);

    bool grew (false);

    // index
    //
    if (j != 0)
    {
      bool is_null (false);
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_image (
        i.index_value, is_null, *j);
      i.index_null = is_null;
    }

    // value
    //
    {
      typedef object_traits< ::UniPAX::Evidence > obj_traits;
      typedef odb::pointer_traits< value_type > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.value_value, is_null, id);
        i.value_null = is_null;
      }
      else
        i.value_null = 1;
    }

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  init (index_type& j,
        value_type& v,
        const data_image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (db);

    // index
    //
    {
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_value (
        j,
        i.index_value,
        i.index_null);
    }

    // value
    //
    {
      typedef object_traits< ::UniPAX::Evidence > obj_traits;
      typedef odb::pointer_traits< value_type > ptr_traits;

      if (i.value_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.value_value,
          i.value_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  insert (index_type i, const value_type& v, void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (di, &i, v);

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    if (!sts.insert_statement ().execute ())
      throw object_already_persistent ();
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  select (index_type& i, value_type& v, void* d)
  {
    using namespace mysql;
    using mysql::select_statement;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (i, v, di, &sts.connection ().database ());

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    select_statement::result r (st.fetch ());
    return r != select_statement::no_data;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  delete_ (void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    sts.delete_statement ().execute ();
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  persist (const container_type& c,
           statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::persist (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  load (container_type& c,
        statements_type& sts)
  {
    using namespace mysql;
    using mysql::select_statement;

    const binding& id (sts.id_binding ());

    if (sts.data_binding_test_version ())
    {
      bind (sts.data_bind (), id.bind, id.count, sts.data_image ());
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    st.execute ();
    auto_result ar (st);
    st.cache ();
    select_statement::result r (st.fetch ());
    bool more (r != select_statement::no_data);

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::load (c, more, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  update (const container_type& c,
          statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::update (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::evidence_traits::
  erase (statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::erase (fs);
  }

  // xref
  //

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  select_statement[] =
  "SELECT "
  "`EntityReference_xref`.`index`, "
  "`EntityReference_xref`.`value` "
  "FROM `EntityReference_xref` "
  "WHERE `EntityReference_xref`.`object_id`=? ORDER BY `EntityReference_xref`.`index`";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  insert_statement[] =
  "INSERT INTO `EntityReference_xref` "
  "(`object_id`, "
  "`index`, "
  "`value`) "
  "VALUES "
  "(?, ?, ?)";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  delete_statement[] =
  "DELETE FROM `EntityReference_xref` "
  "WHERE `object_id`=?";

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  bind (MYSQL_BIND* b,
        const MYSQL_BIND* id,
        std::size_t id_size,
        data_image_type& d)
  {
    using namespace mysql;

    statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    size_t n (0);

    // object_id
    //
    if (id != 0)
      std::memcpy (&b[n], id, id_size * sizeof (id[0]));
    n += id_size;

    // index
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &d.index_value;
    b[n].is_null = &d.index_null;
    n++;

    // value
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &d.value_value;
    b[n].is_null = &d.value_null;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  grow (data_image_type& i,
        my_bool* t)
  {
    bool grew (false);

    // index
    //
    t[0UL] = 0;

    // value
    //
    t[1UL] = 0;

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  init (data_image_type& i,
        index_type* j,
        const value_type& v)
  {
    using namespace mysql;

    statement_kind sk (statement_insert);
    ODB_POTENTIALLY_UNUSED (sk);

    bool grew (false);

    // index
    //
    if (j != 0)
    {
      bool is_null (false);
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_image (
        i.index_value, is_null, *j);
      i.index_null = is_null;
    }

    // value
    //
    {
      typedef object_traits< ::UniPAX::Xref > obj_traits;
      typedef odb::pointer_traits< value_type > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.value_value, is_null, id);
        i.value_null = is_null;
      }
      else
        i.value_null = 1;
    }

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  init (index_type& j,
        value_type& v,
        const data_image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (db);

    // index
    //
    {
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_value (
        j,
        i.index_value,
        i.index_null);
    }

    // value
    //
    {
      typedef object_traits< ::UniPAX::Xref > obj_traits;
      typedef odb::pointer_traits< value_type > ptr_traits;

      if (i.value_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.value_value,
          i.value_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  insert (index_type i, const value_type& v, void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (di, &i, v);

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    if (!sts.insert_statement ().execute ())
      throw object_already_persistent ();
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  select (index_type& i, value_type& v, void* d)
  {
    using namespace mysql;
    using mysql::select_statement;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (i, v, di, &sts.connection ().database ());

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    select_statement::result r (st.fetch ());
    return r != select_statement::no_data;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  delete_ (void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    sts.delete_statement ().execute ();
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  persist (const container_type& c,
           statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::persist (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  load (container_type& c,
        statements_type& sts)
  {
    using namespace mysql;
    using mysql::select_statement;

    const binding& id (sts.id_binding ());

    if (sts.data_binding_test_version ())
    {
      bind (sts.data_bind (), id.bind, id.count, sts.data_image ());
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    st.execute ();
    auto_result ar (st);
    st.cache ();
    select_statement::result r (st.fetch ());
    bool more (r != select_statement::no_data);

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::load (c, more, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  update (const container_type& c,
          statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::update (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::xref_traits::
  erase (statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::erase (fs);
  }

  // name
  //

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  select_statement[] =
  "SELECT "
  "`EntityReference_name`.`index`, "
  "`EntityReference_name`.`value` "
  "FROM `EntityReference_name` "
  "WHERE `EntityReference_name`.`object_id`=? ORDER BY `EntityReference_name`.`index`";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  insert_statement[] =
  "INSERT INTO `EntityReference_name` "
  "(`object_id`, "
  "`index`, "
  "`value`) "
  "VALUES "
  "(?, ?, ?)";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  delete_statement[] =
  "DELETE FROM `EntityReference_name` "
  "WHERE `object_id`=?";

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  bind (MYSQL_BIND* b,
        const MYSQL_BIND* id,
        std::size_t id_size,
        data_image_type& d)
  {
    using namespace mysql;

    statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    size_t n (0);

    // object_id
    //
    if (id != 0)
      std::memcpy (&b[n], id, id_size * sizeof (id[0]));
    n += id_size;

    // index
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &d.index_value;
    b[n].is_null = &d.index_null;
    n++;

    // value
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = d.value_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      d.value_value.capacity ());
    b[n].length = &d.value_size;
    b[n].is_null = &d.value_null;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  grow (data_image_type& i,
        my_bool* t)
  {
    bool grew (false);

    // index
    //
    t[0UL] = 0;

    // value
    //
    if (t[1UL])
    {
      i.value_value.capacity (i.value_size);
      grew = true;
    }

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  init (data_image_type& i,
        index_type* j,
        const value_type& v)
  {
    using namespace mysql;

    statement_kind sk (statement_insert);
    ODB_POTENTIALLY_UNUSED (sk);

    bool grew (false);

    // index
    //
    if (j != 0)
    {
      bool is_null (false);
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_image (
        i.index_value, is_null, *j);
      i.index_null = is_null;
    }

    // value
    //
    {
      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.value_value.capacity ());
      mysql::value_traits<
          value_type,
          mysql::id_string >::set_image (
        i.value_value,
        size,
        is_null,
        v);
      i.value_null = is_null;
      i.value_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.value_value.capacity ());
    }

    if (grew)
      i.version++;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  init (index_type& j,
        value_type& v,
        const data_image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (db);

    // index
    //
    {
      mysql::value_traits<
          index_type,
          mysql::id_ulonglong >::set_value (
        j,
        i.index_value,
        i.index_null);
    }

    // value
    //
    {
      mysql::value_traits<
          value_type,
          mysql::id_string >::set_value (
        v,
        i.value_value,
        i.value_size,
        i.value_null);
    }
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  insert (index_type i, const value_type& v, void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (di, &i, v);

    if (sts.data_binding_test_version ())
    {
      const binding& id (sts.id_binding ());
      bind (sts.data_bind (), id.bind, id.count, di);
      sts.data_binding_update_version ();
    }

    if (!sts.insert_statement ().execute ())
      throw object_already_persistent ();
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  select (index_type& i, value_type& v, void* d)
  {
    using namespace mysql;
    using mysql::select_statement;

    statements_type& sts (*static_cast< statements_type* > (d));
    data_image_type& di (sts.data_image ());

    init (i, v, di, &sts.connection ().database ());

    select_statement& st (sts.select_statement ());
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      grow (di, sts.select_image_truncated ());

      if (sts.data_binding_test_version ())
      {
        bind (sts.data_bind (), 0, sts.id_binding ().count, di);
        sts.data_binding_update_version ();
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  delete_ (void* d)
  {
    using namespace mysql;

    statements_type& sts (*static_cast< statements_type* > (d));
    sts.delete_statement ().execute ();
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  persist (const container_type& c,
           statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::persist (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  load (container_type& c,
        statements_type& sts)
  {
    using namespace mysql;
    using mysql::select_statement;

    const binding& id (sts.id_binding ());

    if (sts.data_binding_test_version ())
    {
      bind (sts.data_bind (), id.bind, id.count, sts.data_image ());
      sts.data_binding_update_version ();
    }

    select_statement& st (sts.select_statement ());
    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      data_image_type& di (sts.data_image ());
      grow (di, sts.select_image_truncated ());

      if (sts.data_binding_test_version ())
      {
        bind (sts.data_bind (), 0, id.count, di);
        sts.data_binding_update_version ();
        st.refetch ();
      }
    }

    bool more (r != select_statement::no_data);

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::load (c, more, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  update (const container_type& c,
          statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::update (c, fs);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::name_traits::
  erase (statements_type& sts)
  {
    using namespace mysql;

    functions_type& fs (sts.functions ());
    fs.ordered_ = true;
    container_traits_type::erase (fs);
  }

  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::discriminator_type
  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  discriminator (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    discriminator_type d;
    {
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        d,
        i.typeid_value,
        i.typeid_size,
        i.typeid_null);
    }

    return d;
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // UtilityClass base
    //
    if (object_traits_impl< ::UniPAX::UtilityClass, id_mysql >::grow (
          i, t + 0UL))
      grew = true;

    // typeid_
    //
    if (t[2UL])
    {
      i.typeid_value.capacity (i.typeid_size);
      grew = true;
    }

    // entityReferenceType
    //
    t[3UL] = 0;

    // entityFeature
    //
    t[4UL] = 0;

    // standardName
    //
    if (t[5UL])
    {
      i.standardName_value.capacity (i.standardName_size);
      grew = true;
    }

    // displayName
    //
    if (t[6UL])
    {
      i.displayName_value.capacity (i.displayName_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // UtilityClass base
    //
    object_traits_impl< ::UniPAX::UtilityClass, id_mysql >::bind (b + n, i, sk);
    n += sk == statement_update ? 1UL : 2UL;

    // typeid_
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_STRING;
      b[n].buffer = i.typeid_value.data ();
      b[n].buffer_length = static_cast<unsigned long> (
        i.typeid_value.capacity ());
      b[n].length = &i.typeid_size;
      b[n].is_null = &i.typeid_null;
      n++;
    }

    // entityReferenceType
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.entityReferenceType_value;
    b[n].is_null = &i.entityReferenceType_null;
    n++;

    // entityFeature
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.entityFeature_value;
    b[n].is_null = &i.entityFeature_null;
    n++;

    // standardName
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.standardName_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.standardName_value.capacity ());
    b[n].length = &i.standardName_size;
    b[n].is_null = &i.standardName_null;
    n++;

    // displayName
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.displayName_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.displayName_value.capacity ());
    b[n].length = &i.displayName_size;
    b[n].is_null = &i.displayName_null;
    n++;
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // UtilityClass base
    //
    if (object_traits_impl< ::UniPAX::UtilityClass, id_mysql >::init (i, o, sk))
      grew = true;

    // typeid_
    //
    if (sk == statement_insert)
    {
      const info_type& di (map->find (typeid (o)));

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.typeid_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.typeid_value,
        size,
        is_null,
        di.discriminator);
      i.typeid_null = is_null;
      i.typeid_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.typeid_value.capacity ());
    }

    // entityReferenceType
    //
    {
      ::boost::shared_ptr< ::UniPAX::EntityReferenceTypeVocabulary > const& v =
        o.entityReferenceType;

      typedef object_traits< ::UniPAX::EntityReferenceTypeVocabulary > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::UniPAX::EntityReferenceTypeVocabulary > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.entityReferenceType_value, is_null, id);
        i.entityReferenceType_null = is_null;
      }
      else
        i.entityReferenceType_null = 1;
    }

    // entityFeature
    //
    {
      ::boost::shared_ptr< ::UniPAX::EntityFeature > const& v =
        o.entityFeature;

      typedef object_traits< ::UniPAX::EntityFeature > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::UniPAX::EntityFeature > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.entityFeature_value, is_null, id);
        i.entityFeature_null = is_null;
      }
      else
        i.entityFeature_null = 1;
    }

    // standardName
    //
    {
      ::std::string const& v =
        o.standardName;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.standardName_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.standardName_value,
        size,
        is_null,
        v);
      i.standardName_null = is_null;
      i.standardName_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.standardName_value.capacity ());
    }

    // displayName
    //
    {
      ::std::string const& v =
        o.displayName;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.displayName_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.displayName_value,
        size,
        is_null,
        v);
      i.displayName_null = is_null;
      i.displayName_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.displayName_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // UtilityClass base
    //
    object_traits_impl< ::UniPAX::UtilityClass, id_mysql >::init (o, i, db);

    // entityReferenceType
    //
    {
      ::boost::shared_ptr< ::UniPAX::EntityReferenceTypeVocabulary >& v =
        o.entityReferenceType;

      typedef object_traits< ::UniPAX::EntityReferenceTypeVocabulary > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::UniPAX::EntityReferenceTypeVocabulary > > ptr_traits;

      if (i.entityReferenceType_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.entityReferenceType_value,
          i.entityReferenceType_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // entityFeature
    //
    {
      ::boost::shared_ptr< ::UniPAX::EntityFeature >& v =
        o.entityFeature;

      typedef object_traits< ::UniPAX::EntityFeature > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::UniPAX::EntityFeature > > ptr_traits;

      if (i.entityFeature_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.entityFeature_value,
          i.entityFeature_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // standardName
    //
    {
      ::std::string& v =
        o.standardName;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.standardName_value,
        i.standardName_size,
        i.standardName_null);
    }

    // displayName
    //
    {
      ::std::string& v =
        o.displayName;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.displayName_value,
        i.displayName_size,
        i.displayName_null);
    }
  }

  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::map_type*
  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::map;

  const access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::info_type
  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::info (
    typeid (::UniPAX::EntityReference),
    0,
    0,
    "UniPAX::EntityReference",
    &odb::create_impl< ::UniPAX::EntityReference >,
    &odb::dispatch_impl< ::UniPAX::EntityReference, id_mysql >,
    0);

  static const access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::entry_type
  polymorphic_entry_for_UniPAX_EntityReference;

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::persist_statement[] =
  "INSERT INTO `EntityReference` "
  "(`unipaxId`, "
  "`unipaxSource`, "
  "`typeid`, "
  "`entityReferenceType`, "
  "`entityFeature`, "
  "`standardName`, "
  "`displayName`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::find_statement[] =
  "SELECT "
  "`EntityReference`.`unipaxId`, "
  "`EntityReference`.`unipaxSource`, "
  "`EntityReference`.`typeid`, "
  "`EntityReference`.`entityReferenceType`, "
  "`EntityReference`.`entityFeature`, "
  "`EntityReference`.`standardName`, "
  "`EntityReference`.`displayName` "
  "FROM `EntityReference` "
  "WHERE `EntityReference`.`unipaxId`=?";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  find_discriminator_statement[] =
  "SELECT "
  "`EntityReference`.`typeid` "
  "FROM `EntityReference` "
  "WHERE `EntityReference`.`unipaxId`=?";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::update_statement[] =
  "UPDATE `EntityReference` "
  "SET "
  "`unipaxSource`=?, "
  "`entityReferenceType`=?, "
  "`entityFeature`=?, "
  "`standardName`=?, "
  "`displayName`=? "
  "WHERE `unipaxId`=?";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::erase_statement[] =
  "DELETE FROM `EntityReference` "
  "WHERE `unipaxId`=?";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::query_statement[] =
  "SELECT\n"
  "`EntityReference`.`unipaxId`,\n"
  "`EntityReference`.`unipaxSource`,\n"
  "`EntityReference`.`typeid`,\n"
  "`EntityReference`.`entityReferenceType`,\n"
  "`EntityReference`.`entityFeature`,\n"
  "`EntityReference`.`standardName`,\n"
  "`EntityReference`.`displayName`\n"
  "FROM `EntityReference`\n"
  "LEFT JOIN `ImportSource` AS `unipaxSource_ImportSource` ON `unipaxSource_ImportSource`.`unipaxId`=`EntityReference`.`unipaxSource`\n"
  "LEFT JOIN `EntityReferenceTypeVocabulary` AS `entityReferenceType_EntityReferenceTypeVocabulary` ON `entityReferenceType_EntityReferenceTypeVocabulary`.`unipaxId`=`EntityReference`.`entityReferenceType`\n"
  "LEFT JOIN `ControlledVocabulary` AS `entityReferenceType_ControlledVocabulary` ON `entityReferenceType_ControlledVocabulary`.`unipaxId`=`entityReferenceType_EntityReferenceTypeVocabulary`.`unipaxId`\n"
  "LEFT JOIN `EntityFeature` AS `entityFeature_EntityFeature` ON `entityFeature_EntityFeature`.`unipaxId`=`EntityReference`.`entityFeature`";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::erase_query_statement[] =
  "DELETE FROM `EntityReference`";

  const char access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::table_name[] =
  "`EntityReference`";

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  persist (database& db, const object_type& obj, bool top, bool dyn)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (top);

    using namespace mysql;

    if (dyn)
    {
      const std::type_info& t (typeid (obj));

      if (t != info.type)
      {
        const info_type& pi (root_traits::map->find (t));
        pi.dispatch (info_type::call_persist, db, &obj, 0);
        return;
      }
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    if (top)
      callback (db,
                obj,
                callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    id_image_type& i (sts.id_image ());
    init (i, obj.unipaxId);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // comment
    //
    {
      ::std::vector< ::std::basic_string< char > > const& v =
        obj.comment;

      comment_traits::persist (
        v,
        esc.comment);
    }

    // memberEntityReferences
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::EntityReference > > const& v =
        obj.memberEntityReferences;

      memberEntityReferences_traits::persist (
        v,
        esc.memberEntityReferences);
    }

    // evidence
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::Evidence > > const& v =
        obj.evidence;

      evidence_traits::persist (
        v,
        esc.evidence);
    }

    // xref
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::Xref > > const& v =
        obj.xref;

      xref_traits::persist (
        v,
        esc.xref);
    }

    // name
    //
    {
      ::std::vector< ::std::basic_string< char > > const& v =
        obj.name;

      name_traits::persist (
        v,
        esc.name);
    }

    if (top)
      callback (db,
                obj,
                callback_event::post_persist);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  update (database& db, const object_type& obj, bool top, bool dyn)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (top);

    using namespace mysql;
    using mysql::update_statement;

    if (dyn)
    {
      const std::type_info& t (typeid (obj));

      if (t != info.type)
      {
        const info_type& pi (root_traits::map->find (t));
        pi.dispatch (info_type::call_update, db, &obj, 0);
        return;
      }
    }

    if (top)
      callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.unipaxId);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // comment
    //
    {
      ::std::vector< ::std::basic_string< char > > const& v =
        obj.comment;

      comment_traits::update (
        v,
        esc.comment);
    }

    // memberEntityReferences
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::EntityReference > > const& v =
        obj.memberEntityReferences;

      memberEntityReferences_traits::update (
        v,
        esc.memberEntityReferences);
    }

    // evidence
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::Evidence > > const& v =
        obj.evidence;

      evidence_traits::update (
        v,
        esc.evidence);
    }

    // xref
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::Xref > > const& v =
        obj.xref;

      xref_traits::update (
        v,
        esc.xref);
    }

    // name
    //
    {
      ::std::vector< ::std::basic_string< char > > const& v =
        obj.name;

      name_traits::update (
        v,
        esc.name);
    }

    if (top)
    {
      callback (db, obj, callback_event::post_update);
      pointer_cache_traits::update (db, obj);
    }
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  erase (database& db, const id_type& id, bool top, bool dyn)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (top);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    if (dyn)
    {
      discriminator_type d;
      root_traits::discriminator_ (sts.root_statements (), id, &d);

      if (d != info.discriminator)
      {
        const info_type& pi (root_traits::map->find (d));

        if (!pi.derived (info))
          throw object_not_persistent ();

        pi.dispatch (info_type::call_erase, db, 0, &id);
        return;
      }
    }

    if (top)
    {
      id_image_type& i (sts.id_image ());
      init (i, id);

      binding& idb (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || idb.version == 0)
      {
        bind (idb.bind, i);
        sts.id_image_version (i.version);
        idb.version++;
      }
    }

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // comment
    //
    comment_traits::erase (
      esc.comment);

    // memberEntityReferences
    //
    memberEntityReferences_traits::erase (
      esc.memberEntityReferences);

    // evidence
    //
    evidence_traits::erase (
      esc.evidence);

    // xref
    //
    xref_traits::erase (
      esc.xref);

    // name
    //
    name_traits::erase (
      esc.name);

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    if (top)
      pointer_cache_traits::erase (db, id);
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  erase (database& db, const object_type& obj, bool top, bool dyn)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (top);

    if (dyn)
    {
      const std::type_info& t (typeid (obj));

      if (t != info.type)
      {
        const info_type& pi (root_traits::map->find (t));
        pi.dispatch (info_type::call_erase, db, &obj, 0);
        return;
      }
    }

    callback (db, obj, callback_event::pre_erase);
    erase (db, id (obj), true, false);
    callback (db, obj, callback_event::post_erase);
  }

  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::pointer_type
  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);
    root_traits::discriminator_type d;

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
      d = root_traits::discriminator (sts.image ());
    }
    else
      root_traits::discriminator_ (sts, id, &d);

    const info_type& pi (
      d == info.discriminator ? info : root_traits::map->find (d));

    pointer_type p (pi.create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback_event ce (callback_event::pre_load);
      pi.dispatch (info_type::call_callback, db, &obj, &ce);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);

      if (&pi != &info)
      {
        std::size_t d (depth);
        pi.dispatch (info_type::call_load, db, &obj, &d);
      }

      sts.load_delayed (0);
      l.unlock ();
      ce = callback_event::post_load;
      pi.dispatch (info_type::call_callback, db, &obj, &ce);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position (), pi.delayed_loader);

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  find (database& db, const id_type& id, object_type& obj, bool dyn)
  {
    ODB_POTENTIALLY_UNUSED (dyn);

    using namespace mysql;

    if (dyn)
    {
      const std::type_info& t (typeid (obj));

      if (t != info.type)
      {
        const info_type& pi (root_traits::map->find (t));
        return pi.dispatch (info_type::call_find, db, &obj, &id);
      }
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  reload (database& db, object_type& obj, bool dyn)
  {
    ODB_POTENTIALLY_UNUSED (dyn);

    using namespace mysql;

    if (dyn)
    {
      const std::type_info& t (typeid (obj));

      if (t != info.type)
      {
        const info_type& pi (root_traits::map->find (t));
        return pi.dispatch (info_type::call_reload, db, &obj, 0);
      }
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.unipaxId);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  load_ (statements_type& sts,
         object_type& obj,
         bool reload)
  {
    ODB_POTENTIALLY_UNUSED (reload);

    extra_statement_cache_type& esc (sts.extra_statement_cache ());

    // comment
    //
    {
      ::std::vector< ::std::basic_string< char > >& v =
        obj.comment;

      comment_traits::load (
        v,
        esc.comment);
    }

    // memberEntityReferences
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::EntityReference > >& v =
        obj.memberEntityReferences;

      memberEntityReferences_traits::load (
        v,
        esc.memberEntityReferences);
    }

    // evidence
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::Evidence > >& v =
        obj.evidence;

      evidence_traits::load (
        v,
        esc.evidence);
    }

    // xref
    //
    {
      ::std::vector< ::boost::shared_ptr< ::UniPAX::Xref > >& v =
        obj.xref;

      xref_traits::load (
        v,
        esc.xref);
    }

    // name
    //
    {
      ::std::vector< ::std::basic_string< char > >& v =
        obj.name;

      name_traits::load (
        v,
        esc.name);
    }
  }

  void access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  discriminator_ (statements_type& sts,
                  const id_type& id,
                  discriminator_type* pd)
  {
    using namespace mysql;

    id_image_type& idi (sts.discriminator_id_image ());
    init (idi, id);

    binding& idb (sts.discriminator_id_image_binding ());
    if (idi.version != sts.discriminator_id_image_version () ||
        idb.version == 0)
    {
      bind (idb.bind, idi);
      sts.discriminator_id_image_version (idi.version);
      idb.version++;
    }

    discriminator_image_type& i (sts.discriminator_image ());
    binding& imb (sts.discriminator_image_binding ());

    if (i.version != sts.discriminator_image_version () ||
        imb.version == 0)
    {
      MYSQL_BIND* b (imb.bind);
      std::size_t n (0);
      {
        b[n].buffer_type = MYSQL_TYPE_STRING;
        b[n].buffer = i.discriminator_value.data ();
        b[n].buffer_length = static_cast<unsigned long> (
          i.discriminator_value.capacity ());
        b[n].length = &i.discriminator_size;
        b[n].is_null = &i.discriminator_null;
      }

      sts.discriminator_image_version (i.version);
      imb.version++;
    }

    {
      select_statement& st (sts.find_discriminator_statement ());
      st.execute ();
      auto_result ar (st);
      select_statement::result r (st.fetch ());

      if (r == select_statement::no_data)
      {
        throw object_not_persistent ();
      }
      else if (r == select_statement::truncated)
      {
        bool grew (false);
        my_bool* t (sts.discriminator_image_truncated ());

        if (t[0UL])
        {
          i.discriminator_value.capacity (i.discriminator_size);
          grew = true;
        }

        if (grew)
          i.version++;

        if (i.version != sts.discriminator_image_version ())
        {
          MYSQL_BIND* b (imb.bind);
          std::size_t n (0);
          {
            b[n].buffer_type = MYSQL_TYPE_STRING;
            b[n].buffer = i.discriminator_value.data ();
            b[n].buffer_length = static_cast<unsigned long> (
              i.discriminator_value.capacity ());
            b[n].length = &i.discriminator_size;
            b[n].is_null = &i.discriminator_null;
          }

          sts.discriminator_image_version (i.version);
          imb.version++;
          st.refetch ();
        }
      }
    }

    if (pd != 0)
    {
      discriminator_type& d (*pd);
      {
        mysql::value_traits<
            ::std::string,
            mysql::id_string >::set_value (
          d,
          i.discriminator_value,
          i.discriminator_size,
          i.discriminator_null);
      }
    }
  }

  result< access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::object_type >
  access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += "\n";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        true,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::polymorphic_object_result_impl<object_type> > r (
      new (shared) mysql::polymorphic_object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::UniPAX::EntityReference, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
